/goal Execute the Lliam-GOV build through its Phase-5 exit (Phases 3→4→5), shipping clean per-issue PRs against main. Lliam-GOV is a governance-hardened Python fork of Hermes Agent serving as ISO 42001 / CMMC L2 / ISO 27001 evidence. Plan is approved; your job is execution, not redesign.

WORKING DIR: /Volumes/WORKSPACE/1-Projects/lliam-gov (GitHub jdavis-cyber/lliam-gov).
NEVER edit /Volumes/WORKSPACE/1-Projects/lliam_ai_agent — that's dormant Lliam-OPS, reference only.

READ FIRST, IN FULL, BEFORE ANY EDIT (they contain the complete spec, sequence, acceptance criteria, and operating rules):
1. handoffs/2026-05-30-Lliam-GOV-Goal-Handoff.md  ← the authoritative handoff
2. docs/plans/2026-05-30-lliam-gov-completion-plan.md  ← the approved plan
3. /Volumes/WORKSPACE/0-Orientation/Hermes-to-Lliam-ISO42001-Plan-COMPREHENSIVE-v3-2026-05-25.docx  ← design of record (§5 overlay, §9 phases)
4. docs/governance/control-matrix.md + evidence/control-matrix.csv  ← evidence ledger
Open Brain memory: list_thoughts then search_thoughts "lliam_gov" before working; capture after each slice (tag lliam_gov).

STATE: main @ 89cc536, CI green. Phases 0–2 Done, Phase 3 ~70% done. Noise floor (must stay ⊆): 11 files / 36 fails under --extra all.

DO, in order — one issue → one branch (claude/ai-NNN-slug) → one PR into main:
- Phase 3: AI-214 (gateway auth audit) · AI-215 (EncryptedFile abstraction — route ~/.lliam-gov persisted state through AES-256-GCM via key_manager; THE linchpin) · AI-216 (rotate-key + audit export-aep CLI) · AI-217 (24h smoke + flip matrix rows = exit gate) → close AI-195.
- Phase 4: AI-218..224 (auth/authz, egress+TLS, runtime_guard, self-improve approval gate, CUI marking-only) → close AI-196.
- Phase 5: AI-225, AI-226, AI-227, AI-229 (chaos) → close AI-197.

DO NOT (hard carve-outs, Jerome 2026-05-30 — these gate nothing): SKIP AI-228 (pen-test). DO NOT touch Phase 6 / AI-234 / Katmai install or IT review — they wait on Katmai's commitment + the D9 swap decision. Build only through Phase 5 minus AI-228, then stop and report.

GATES (need Jerome, not you): no git push / no PR merge without his explicit instruction — open PRs and report, he self-merges. Don't delete remote branches; don't enable Linear estimation. Brief him on any design ambiguity.

ENV: export UV_PROJECT_ENVIRONMENT=/Users/just_jerome/.venvs/lliam-gov; tests = uv run python scripts/run_tests_parallel.py (focused: uv run pytest -q tests/lliam_gov/<f>); dev FIPS override LLIAM_GOV_ALLOW_NON_FIPS=1; before every commit run: find . -name '._*' -type f ! -path './.git/*' -delete. Keep internal hermes package names (facelift only); new code under lliam_gov/security/.

DISCIPLINE: never flip a matrix current_state row until code AND evidence both exist. Linear (key in ~/.codex/ea-routines/env): set In Progress on start, In Review on PR open, Done only after merge + evidence; never delete; parents close after all leaves Done. Stop and ask Jerome at each phase exit.
