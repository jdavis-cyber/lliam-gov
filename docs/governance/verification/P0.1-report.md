# P0.1 — Verification Report

| Field | Value |
|---|---|
| Step | P0.1 — Overlay scaffold + master strict-posture switch |
| Linear | AI-506 |
| Controls | LG-CH-01, LG-CH-02, LG-CH-03 (scaffold), LG-CH-09 |
| Branch | `harden/phase-0-baseline` |
| Commit | `c5ba12e` — feat(gov): additive cli-config.gov overlay + strict posture switch |
| Date | 2026-06-29 |
| Result | **PASS** |

## Objective
Stand up the additive governance-overlay backbone + the single strict-posture switch.

## Key finding
`HERMES_CONFIG` was in the env allowlist (`config.py:198`) but **no shipped code consumed it as a config-file selector**. Added that wiring additively (`config._gov_overlay_path`) so the overlay model + the documented rollback ("unset/repoint `HERMES_CONFIG`") are real.

## What was tested
1. Strict posture loads via `HERMES_CONFIG` and `posture_resolver` runs.
2. The switch alone coerces fail-open guards to fail-closed; no-op when permissive.
3. Config cache/merge regression (core `_load_config_impl` path was edited).
4. Capability preserved under the overlay.

## Results (actual)
- Effective config under overlay: `security.posture=strict`, `tirith_fail_open=False`, `allow_lazy_installs=False`, `deny_on_no_approver=True`, `posture_findings_blocking=True` → **PASS**.
- Switch coercion proven on fail-OPEN defaults: `tirith_fail_open True→False`, `allow_lazy_installs True→False`, `allow_private_urls None→False`, `deny_on_no_approver None→True`. Permissive config (no posture) → untouched (low-side no-op).
- Regression (manual, pytest not in runtime venv — installing it would touch the live install): no-overlay cached load, readonly identity, deepcopy equality/independence, overlay-over-base preserves model/provider, overlay-edit cache invalidation, rollback → **all PASS**.
- Capability: agent read a file and wrote a correct `power.py` under the overlay.
- Evidence: posture-resolution line present in the real `agent.log` (LG-CH-02).

## Expected-deny / expected-block cases
None (this step renders the coherent fail-closed config + the LG-CH-09 blocking signal; the blocking *enforcement* lands in the later drift-gate step).

## Capability preservation
Agent codes normally under the overlay; low-side deployments unaffected (resolver is a strict no-op).

## Artifacts
`cli-config.gov.yaml` (with reserved git-tag pin line), `hermes_cli/posture_resolver.py` (NEW), `hermes_cli/config.py` (additive wiring), `docs/governance/baseline-critical-keys.yaml` (NEW).

## Outstanding
PR — held until the Phase-0 verify gate (Rule 4).
