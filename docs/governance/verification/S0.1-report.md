# S0.1 — Verification Report

| Field | Value |
|---|---|
| Step | S0.1 — Clone the fork + capture pre-hardening baseline |
| Linear | AI-504 |
| Branch | `harden/phase-0-baseline` |
| Commit | `63999e6` — chore(gov): isolated hardening workspace + backups + baseline/regression records |
| Date | 2026-06-29 |
| Result | **PASS** (setup + baseline) |

## Objective
Set up an isolated hardening workspace and safety nets before changing any code.

## What was tested
- Clone present and on the phase branch.
- State isolation (`HERMES_HOME` per-shell only, never global / never in `~/.zshrc`).
- Backups of the live app exist.
- CLI runs from the clone **without** code changes.
- Capability smoke: trivial coding task + file read.

## Results (actual)
- Clone: `~/dev/lliam-gov` (full Hermes/Lliam tree), branch `harden/phase-0-baseline` off `main` (`a63f9e9`).
- Isolation: `HERMES_HOME=~/dev/lliam-gov-home`; confirmed no `HERMES_*` in `~/.zshrc` or ambient env. (Bash tool spawns a fresh shell per call, so `HERMES_HOME` is prefixed per-invocation — identical isolation guarantee.)
- Backups: `~/.hermes` → `~/.hermes.pre-harden-backup` (3.6G, verified); `~/Library/Application Support/Lliam-GOV` → `…/Lliam-GOV.pre-harden-backup` (2.0M, verified).
- CLI: `hermes --help` and `hermes status` respond. Provider `OpenAI Codex` (OAuth); default model `gpt-5.4`.
- **Capability smoke PASS:** agent read `sample.py`, wrote a correct `multiply.py`, replied `DONE` (exit 0); `multiply(6,7)=42` verified.

## Expected-deny / expected-block cases
None at this step (setup + baseline capture only).

## Capability preservation
Agent fully functional pre-hardening (file read + coding task verified).

## Records produced
- `docs/governance/_baseline-capability-note.md` (what works today)
- `docs/governance/regression-log.md` (regression table)

## Outstanding (per issue acceptance)
- PR against `jdavis-cyber/lliam-gov` — held until the Phase-0 verify gate is green (Rule 4: tag + push after the phase gate).
