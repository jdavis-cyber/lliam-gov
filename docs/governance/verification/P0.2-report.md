# P0.2 — Verification Report

| Field | Value |
|---|---|
| Step | P0.2 — Flip permissive defaults to the CUI floor + env profile |
| Linear | AI-508 |
| Controls | LG-CH-07 (+ LG-AZ-04, LG-DP-03/04, LG-HO-04, LG-SC-06) |
| Branch | `harden/phase-0-baseline` |
| Commit | `a711063` — feat(gov): CUI-floor value overrides + .env.gov profile |
| Date | 2026-06-29 |
| Result | **PASS** |

## Objective
Pin permissive shipped defaults to the CUI floor (existing keys) + the env-layer profile.

## What was tested
1. Effective config resolves to the strict CUI-floor values.
2. File-write confinement: write outside `HERMES_WRITE_SAFE_ROOT` refused; inside succeeds.

## Results (actual)
- **Effective config 9/9 PASS:** `security.posture=strict`, `redact_secrets=True`, `tirith_fail_open=False`, `allow_lazy_installs=False`, `privacy.redact_pii=True`, `memory.write_approval=True`, `skills.write_approval=True`, `skills.guard_agent_created=True`, `skills.inline_shell=False`.
- **Write confinement (authoritative gate `is_write_denied`):** with `HERMES_WRITE_SAFE_ROOT` set → inside-root **allowed**, outside-root **denied**; with it unset → baseline-permissive (both allowed). Proves the env lever activates confinement.
- **Write confinement (end-to-end, real `file_write` tool):** agent wrote `inside.py` inside the safe root (succeeded) and the outside write was **refused** (`"Write denied"`); filesystem confirms the outside file was never created.

## Expected-deny / expected-block cases
- Write to a path outside `HERMES_WRITE_SAFE_ROOT` → **refused** (expected; LG-AZ-04).

## Capability preservation
Agent writes freely within its workspace; only out-of-bounds writes are refused. `write_approval` only stages/delays memory+skill writes (ordinary file writes are not gated); `redact_pii` only masks PII to providers.

## Artifacts
`cli-config.gov.yaml` (LG-CH-07 value flips), `.env.gov.example` (NEW — `HERMES_WRITE_SAFE_ROOT`, `HERMES_REDACT_SECRETS`, `API_SERVER_KEY=` placeholder + bridges).

## Outstanding
PR — held until the Phase-0 verify gate (Rule 4).
