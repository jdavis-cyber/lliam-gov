# P0.7 — Verification Report

| Field | Value |
|---|---|
| Step | P0.7 — CI guards + governance docs (godmode/drift/path-freeze gates) |
| Linear | AI-514 |
| Controls | LG-CH-06, LG-CH-03/09, LG-SD-01, LG-SS-01, LG-GV-02, LG-CH-08 |
| Branch | `harden/phase-0-baseline` |
| Commit | HEAD of `harden/phase-0-baseline` — ci(gov): godmode/drift/path-freeze gates + governance docs |
| Date | 2026-06-30 |
| Result | **PASS** |

## Objective
Add the CI preflight gates (godmode, baseline-drift, path-freeze, deps pin+hash) and the foundational governance docs (intended-use, deviation register).

## What was tested (locally)
1. Godmode/prohibited-skill gate — pass when disabled, fail when force-enabled.
2. Baseline-drift gate — pass on real overlay, fail-closed on critical drift.
3. Path-freeze guard — pass on the real (additive) PR diff, trip on a frozen-path rename.
4. Dependency pin+hash gate.

## Results (actual)
- **Godmode gate (LG-CH-06):** real overlay → **PASS** (godmode/obliteratus/sherlock/web-pentest/oss-forensics all denied). Force-enabled (godmode removed from `skills.disabled`, isolated from the policy file) → **FAIL** exit 1: `Prohibited skill 'godmode' is NOT denied`. (Also confirmed defense-in-depth: the gate reads BOTH the overlay and `policy/skills-allowlist.yaml`.)
- **Baseline-drift gate (LG-CH-03/09):** real overlay → **PASS** (14 `P0*` critical/active keys match; effective config via `load_config` = DEFAULT+home+overlay+posture_resolver). Tampered `terminal.backend: local` → **FAIL** exit 1: `CRITICAL baseline drift … terminal.backend: expected 'docker', got 'local'`. Confirmed posture_resolver does not mask non-coherence keys.
- **Path-freeze guard (LG-SD-01):** real PR diff `main...HEAD` → **PASS** (29 frozen paths guarded; all my changes are additive/modify). Throwaway rename `tools/approval.py → approval_RENAMED.py` → **FAIL** exit 1: `RENAME of frozen path`. Cleaned up — `tools/approval.py` intact, branch clean.
- **Deps pin+hash gate (LG-SS-01):** **PASS** — `uv.lock` 1798 sha256 hashes; all `[project].dependencies` upper-bounded; `uv sync --locked` consistent.

## Expected-deny / expected-block cases
- Prohibited skill enabled in the gov profile → build **fails**.
- Critical config key drifted from baseline → build **fails closed**.
- PR renaming/deleting a frozen module/CLI surface → build **fails**.
- Unpinned/unhashed dependency → build **fails**.

## Capability preservation
Gates are CI-only — no effect on the agent runtime. The drift + godmode gates are no-ops on a non-gov profile; the path-freeze guard only blocks RENAME/DELETE (additive overlays always pass). A time-boxed, approver-bound waiver path is documented for capability-preserving exceptions.

## Artifacts
`ci/governance-ci-gates/{prohibited_skills_gate.sh, baseline_drift_gate.py, path_freeze_gate.py, path-freeze-manifest.yaml, deps_hash_gate.sh}`, `.github/workflows/governance-gates.yml` (NEW standalone), `docs/governance/{intended-use.md, deviation-register.md, deviation-register.yaml}`, updated `baseline-critical-keys.yaml`.

## Notes
- `governance-gates.yml` is standalone (on PR/push) so the frozen `ci.yml` is untouched. Wiring it into `ci.yml`'s `all-checks-pass` required-check + branch protection is a follow-up (a GitHub setting, not code).
- This step opens the Phase-0 PR so the gates run in CI (per the per-phase PR convention).

## Outstanding
PR opened (Phase-0). CI run result + the P0.V phase verify gate, then tag `gov-P0-good`.
